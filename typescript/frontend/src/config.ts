// --------------------------------------------------------
// Environment Setup
// --------------------------------------------------------
export const config = {
};

if (process.env.NODE_ENV === 'development') {
  console.log('Environment variables:', config);
}
